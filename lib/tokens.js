// tokens.js — Layer 1: primitives (spacing, radius, border, elevation).
// Per CLAUDE.md styling rule: raw style values live here, composed into
// semantic styles in components.js. No raw values inside components.
// SCAFFOLD ONLY — do not populate yet (Step 2 onward).

export {};
