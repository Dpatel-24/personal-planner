// components.js — Layer 2: semantic styles (card, panel, input, buttonPrimary)
// composed from the primitives in tokens.js. Components consume these, never
// raw token/style values. Shadow only on overlay/interactive elements.
// SCAFFOLD ONLY — do not populate yet (Step 2 onward).

export {};
